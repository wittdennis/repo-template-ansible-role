repos:
  - repo: https://github.com/ansible-community/ansible-lint.git
    rev: v26.2.0
    hooks:
      - id: ansible-lint
        files: ^(?!\.).*\.(yaml|yml)$
        verbose: true
        args: ["-p", "./"]
